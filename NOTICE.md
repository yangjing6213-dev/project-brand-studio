# Notices and asset rights

The MIT License applies only to BrandLoom source code and documentation. It does not grant any right in bundled company logos, IP reference images, or generated example imagery.

Every distributable image has an adjacent provenance record. The built-in ENHE logo and the `author-anime`, `tuotuo`, and `xingbi` IP references are user-provided project assets, with `user_authorized` public-skill-package provenance recorded beside each asset. The legacy baseline examples in `docs/examples/` are deterministic project-authored synthetic illustrations; the two `brandloom-host-*` examples are AI-assisted host-tool outputs composed with Pillow. All examples have matching provenance records, and the README currently showcases only the latter pair.

The author profile card in `docs/images/` is an exact user-provided attachment authorized for public repository documentation, with its distribution scope and SHA-256 recorded in the adjacent provenance file.

Third-party commercial reference posters are not included in this repository or release package. Their source hashes are not available, so the package builder provides an optional denylist interface for verified forbidden hashes; it intentionally ships with no invented denylist values.
