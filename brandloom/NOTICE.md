# Notices and asset rights

The MIT License in this package applies only to BrandLoom source code and
documentation. It does not grant rights in bundled company logos, IP reference
images, or generated example imagery.

Every packaged raster asset is decoded during the build and must have adjacent
provenance with `user_authorized` and `public_skill_package` status plus a
matching SHA-256. Third-party commercial reference posters are not included.

The public package also contains four exact user-authorized files recorded at
the current-turn confirmation: `拓拓与星比正视图.png` (shared primary
appearance), `拓拓五视图.png` and `星比五视图.png` (supplemental geometry),
and `白色 ENHE LOGO.png` (explicit white-logo variant). Their source
filenames, SHA-256 digests, confirmation time, and roles are stored in adjacent
same-stem provenance JSON files. The white variant is opt-in and requires a
contrast review; no pixel cleanup or geometry change was performed.
