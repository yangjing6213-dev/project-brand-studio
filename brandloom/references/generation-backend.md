# 图片生成后端边界

`GENERATION_READY` 是唯一允许调用图片工具的状态，且必须已经得到用户明确确认。

- 仅使用当前宿主提供的内置图片生成/编辑工具，并使用工具返回的路径 exactly。
- 不读取或请求 API key，不切换 Images API、SDK、第三方 provider 或递归 Codex。
- 工具失败、空路径、路径缺失、图片不可读或尺寸严重不匹配时立即 hard-stop，并保留已确认计划。
- 不得在失败后自动重试；如需重试，必须回到确认流程并取得用户选择。
- 底图不得包含可读最终文案或可见公司 LOGO；后续由 Pillow 确定性合成。
