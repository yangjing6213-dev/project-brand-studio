# 字体预设

字体是组合而非单一字体，Skill 不打包或分发任何字体文件。预设：微软雅黑 + Segoe UI；思源黑体 + Inter；HarmonyOS Sans + Inter；阿里妈妈数黑体 + Montserrat；得意黑 + Space Grotesk。后两组仅在本机存在且许可满足时可选。

解析顺序：用户确认字体 → 项目指定路径 → 系统字体目录 → 用户确认的替代字体。缺少字体时阻塞，必须由用户确认回退，禁止静默替换；溢出则返回文案或字体阶段，不删除已确认文案。
