# 上下文分析

只分析当前真实可访问材料：用户消息、当前会话可访问历史、可读取附件、用户提供的公开链接、workspace README/文档/截图/品牌资产和已有 `.brandloom/brand-brief.json`。不可访问旧对话时明确说明，不声称已读取；请求用户上传导出、粘贴说明、给公开链接、上传 README/截图，或仅按当前简述分析。

输出：项目名称、类型、核心功能、目标用户、痛点、使用结果、可验证依据、推断项、未确认信息、附件/链接清单、已有品牌资产。文案证据标记 `verified`、`user_supplied`、`inferred`、`unsupported` 或 `conceptual_mockup`；unsupported 不进入正式宣传文案，不虚构用户数、性能、安全分数、发布状态或未实现功能。
